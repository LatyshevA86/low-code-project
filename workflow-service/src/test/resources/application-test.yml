spring:
  temporal:
    start-workers: false
    connection:
      target: localhost:7233
  jpa:
    show-sql: false

app:
  activities:
    ai:
      enabled: false
    rest:
      enabled: false

logging:
  level:
    root: warn
    ru.latyshev.workflow: info
